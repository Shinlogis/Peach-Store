<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Product">

	
	<insert id="insert" parameterType="Product">
		insert into product(product_code, product_name, price, introduce, detail)
		values(#{product_code}, #{product_name}, #{price}, #{introduce},#{detail})
		
		<!-- insert 시키자 마자, 현재 세션에서 일으킨 pk의 최대값  -->
		<selectKey keyColumn="product_id" keyProperty="product_id" resultType="int" order="AFTER">
			select last_insert_id() as product_id
		</selectKey>
	</insert>

	<sql id="columns">
		product_id, product_code, product_name, price, introduce, detail
	</sql>

	<select id="selectAll" resultType="Product">
		select <include refid="columns"/> from product
	</select>
	
	<select id="select" parameterType="int" resultType="Product">
		select <include refid="columns"/> from product 
		where product_id=#{product_id}
	</select>
	
</mapper>


















