<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OrderReceipt">

	

	<select id="select" parameterType="OrderReceipt" resultType="OrderReceipt">
		select order_receipt_id, order_date, order_status, user_id 
		 from order_receipt where order_receipt_id = #{order_receipt_id} and user_id = #{user.user_id}
	</select>
	
</mapper>